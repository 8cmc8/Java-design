模版方法模式：
在一个方法中定义一个算法的骨架，而将一些步骤延迟到子类中。模版方法使得子类可以在不改变算法结构的情况下，重新定义算法中的某些步骤。

策略模式和模版方法模式都封装方法，一个用组合，一个用继承。

Arrays.sort()方法就包含了模版方法模式的思想：
通常我们无法设计一个类继承Java数组，而sort()方法希望能够适用于所有的数组而被设计为静态方法，
并由被排序的对象内的每个元素自行提供比较大小的算法部分（需要实现Comparable接口的compareTo方法），
由于不需要继承数组就能实现这个算法，使得排序变得更有弹性。

设计原则：
1、好莱坞原则：高层组件控制何时以及如何让低层组件参与，低层组件绝不可以直接调用高层组件。